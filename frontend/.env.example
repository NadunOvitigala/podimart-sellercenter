# Leave VITE_API_URL empty to use the local API (port 8001).
# After AWS deploy, paste the stack outputs here and restart npm run dev.
VITE_API_URL=
VITE_PUBLIC_URL=http://localhost:5173
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_REGION=ap-south-1
